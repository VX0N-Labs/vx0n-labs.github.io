[
{{- $comma := false -}}
{{- range .Site.RegularPages -}}
{{- if .Params.hidden }}{{ continue }}{{ end -}}
{{- if $comma }},{{ end }}
{
    "title": {{ .Title | jsonify }},
    "description": {{ .Params.description | jsonify }},
    "url": {{ .RelPermalink | jsonify }},
    "tags": {{ .Params.tags | jsonify }},
    "redirect": {{ .Params.redirect | jsonify }}
}
{{- $comma = true -}}
{{- end }}
]